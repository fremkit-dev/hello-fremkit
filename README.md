# hello-fremkit

A minimal Express.js hello-world app used as the default demo repository for Fremkit. Point Fremkit at this repo to see a cloud dev environment spin up automatically — no config needed.
